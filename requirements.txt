# Зависимости управляются через uv (см. pyproject.toml + uv.lock).
# Этот файл оставлен для совместимости / ручной установки без uv:
#   pip install -r requirements.txt
#
# Канонический источник — pyproject.toml. При расхождении правьте его и `uv lock`.

fastembed>=0.5,<0.9
numpy>=1.24,<3
fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.35
httpx>=0.27,<0.29
python-dotenv>=1.0,<2.0
